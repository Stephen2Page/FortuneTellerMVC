@model IEnumerable<FortuneTellerMVC.Models.Saving>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.SavingsAmount)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.SavingsAmount)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.SavingsID }) |
            @Html.ActionLink("Details", "Details", new { id=item.SavingsID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.SavingsID })
        </td>
    </tr>
}

</table>
