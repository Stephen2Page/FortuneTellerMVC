@model IEnumerable<FortuneTellerMVC.Models.Transportation>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Transportation1)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Transportation1)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TransportationID }) |
            @Html.ActionLink("Details", "Details", new { id=item.TransportationID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TransportationID })
        </td>
    </tr>
}

</table>
